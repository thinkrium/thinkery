<?php

temp variable holding smallest number
temp variable holding the 2nd smallest number_format

temp variable holding the key
temp variable holding the 2nd key

temp variable holding an empty array to be returned

smallest fixed number to 1

go through each element compare the first small number to the element in the array if 
    
if smallest number is smaller than every number set and is not 1 
    set to smallest fixed number
    
    if is smallest fixed number set to smallest number
        

if the second smallest number is larger than the smllest number but smller than 
    every other number 
    or if it is set to smallest number as well than set it to smallest fixed number++
        
        if it is already smallest fixed number++ than set it to the smallest fixed number